#include "ircserv.hpp"

// Check if client is in the specified channel
// Check if the channel works on invits
// Check if the channel exists
// Check if the target exists
// Check if the target is already in the channel

void	INVITE( t_server *serv, int clientFd, std::string channelName, std::string arg )
{
	std::cout << "arg-" << arg << "-" << std::endl;
	return ;
}
